# Georgia Appellate Watch -- case queue (inbox)
#
# Add a case to the feed by pasting its CourtListener opinion URL on its own line
# below and committing this file. A push to queue.txt runs the case through the
# funnel and opens a pull request for your review; nothing goes live until you
# merge it. You can also run it by hand from Actions > "Georgia Appellate Watch
# queue" (with "resolve only" first to check what a line resolves to).
#
# One case per line. Accepted forms:
#   https://www.courtlistener.com/opinion/10862301/state-farm-v-.../   (paste the URL)
#   10862301                                                            (bare cluster id)
#   10862301:gactapp                                                    (cluster:court, if the
#                                                                        court will not auto-derive;
#                                                                        court in ga|gactapp|ca11|scotus)
#
# Add a trailing "!" to force a case past the relevance gate (it is still subject
# to your PR review):
#   10862301 !
#
# Anything after "#" is a comment. Each case is screened: a case judged out of
# scope is reported in the PR rather than carded (force with "!" to override).
#
# After a run this file is drained automatically: carded, gated-out, declined, and
# already-on-the-site lines are removed and reported in the PR; lines deferred on the
# CourtListener budget stay to retry; unresolved lines are parked here as comments
# with the reason, for you to fix or delete.
#
# --- queue below this line ---
# (Cleared 2026-08-23: the orphaned comment fragments that sat here were the tails of the two
# Alabama escalations from issue #191 -- Ex parte ACCA Liability Self-Insurance Fund SC-2025-1015
# and Ex parte State Farm SC-2025-0918. Both are carded and live; the drain removed their active
# lines but left these indented continuations behind.)

# Screen-drop audit, issue #283 -- the first run of the screen-stage smell test flagged 22 drops
# whose logged reason states no recognized disqualifier. Verified against the opinions before
# queueing, and only what survives that read goes below.
#
# NOT forced. The earlier escalations carried "!" because they were TRIAGE drops -- triage had
# already read the full opinion and said no, so only a force could get a second look. This is a
# SCREEN drop, and the queue skips the Tier-1 screen entirely, so triage has never seen this case
# at all. Queueing it plain gets it the full-text read the caption guess denied it. Forcing would
# skip the one check that has not yet happened.

# Fla. Dept. of Environmental Protection & Fla. DOT v. Estate of Zoll (Fla. 5th DCA, 2026-07-31).
# Wrongful death of a pedestrian struck crossing SR A1A between two state-park campgrounds.
# Reverses the denial of the agencies' motion to dismiss on SOVEREIGN IMMUNITY: siting the
# campgrounds was a discretionary, planning-level decision, and the agencies did not create a
# known dangerous condition that was not readily apparent to someone who could be injured by it.
# Published, and it decides the immunity question rather than applying it to settled facts.
# Screen dropped it as "Probate/estate dispute" -- read straight off "the Estate of" in the
# caption. Nothing about the case is probate.
#
# The description sits ABOVE the entry, not as a trailing continuation, so the drain removes one
# self-contained line and takes the whole note with it. Trailing continuations are what left the
# orphaned fragments cleared above.
#
# COURT PINNED EXPLICITLY. As a bare id this line could not be screened: the queue run on
# 2026-08-23 resolved the cluster fine (3 CourtListener calls) but derived court '?', and gated it
# "out of scope: court '?' not in {ala, alacivapp, ca11, fla, fladistctapp, ga, gactapp, scotus}",
# then parked the line with instructions to name a court. Derivation reads the court off the
# docket, and Florida's DCAs report per-district ids that are not the supported token. The feed
# does cover this court -- jurisdictions.COURT_MAP maps fladistctapp -> dcafl, and dcafl is exactly
# what the screen logged when it dropped this case. So the id is supplied here as the fallback
# seed_result takes when derivation fails.
10937583:fladistctapp
